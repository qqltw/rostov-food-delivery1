meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build

run:
  command: npm run db:repair-platform-migration && npm run db:deploy && NODE_ENV=production npm run start
  containerPort: 3000
